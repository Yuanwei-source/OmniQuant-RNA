"""Stability analysis (decontam perturbation + leave-one-out subsampling).
Results already computed in benchmark_results/.
This script reads existing output and writes manuscript-ready tables."""
import sys; print("Placeholder: stability numbers already in benchmark report")
